import { IvyShell } from "./_components/ivy-shell";

/** IVY DataEntry monitor — the Telegram → OCR → Spoke pipeline (Supabase
 *  ivy_scans). Overview · Scans · Activity, real-time. Separate from the web
 *  OCR monitor. */
export default function IvyMonitorPage() {
  return <IvyShell />;
}
